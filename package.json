{
  "name": "jp-custom-gear",
  "version": "0.1.0",
  "private": true,
  "description": "JP Custom Gear site with Stripe Checkout",
  "scripts": {
    "test": "node --test \"tests/*.test.js\""
  },
  "dependencies": {
    "stripe": "^17.0.0",
    "resend": "^4.0.0"
  },
  "engines": {
    "node": "20.x"
  }
}
